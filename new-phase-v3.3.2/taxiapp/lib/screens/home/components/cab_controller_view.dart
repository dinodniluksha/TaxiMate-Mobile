import 'package:flutter/material.dart';

class CabControllerView extends StatelessWidget {
  const CabControllerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 500,
      color: Colors.pinkAccent,
    );
  }
}
